# Scrapbook photo collage repository rule

When the user asks for a scrapbook, journal, travel-diary, memory-board, or collage image, read the repository's `SKILL.md` and apply it. Load linked references only when relevant. Do not apply this workflow to unrelated tasks.
